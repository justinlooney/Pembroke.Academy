# Mixamo inbox — paste Drive links here, commit, and the workflow does the rest.
#
# Edit this file (GitHub's web editor on a phone is fine), commit it, and
# .github/workflows/mixamo.yml fetches every link, builds the characters,
# shrinks them, and commits the result to assets/.
#
# Each block starts with "# out:" naming what to build, and lists the
# downloads that go into it. THE FIRST ENTRY IN A BLOCK CARRIES THE MESH.
#
# Two shapes both work, so use whichever you actually downloaded:
#
#   one character, several clips — the download WITH SKIN first, then
#   animation-only files. One mesh, every motion, lightest on the wire.
#
#   several characters, each with its motion baked in — one block each,
#   one entry each. Heavier, but it is what you get from just hitting
#   Download on a few different characters.
#
# The name becomes the clip name; underscores become spaces.
# Share every file as "Anyone with the link" or the fetch will say so.
# Tick "In Place" on Mixamo where it is offered: the campus drives
# playback from how fast a student is really walking, so a clip that also
# carries travel fights it and the feet skate.
#
# Uncomment a block and swap in your links. Nothing runs until you do.

# ── one character who can do several things ──────────────────────────
# out: assets/stu_walker.glb
# character       https://drive.google.com/file/d/XXXXXXXX/view
# Walking         https://drive.google.com/file/d/XXXXXXXX/view
# Standing_Idle   https://drive.google.com/file/d/XXXXXXXX/view
# Slow_Run        https://drive.google.com/file/d/XXXXXXXX/view

# ── or a character who arrived with their motion already on ──────────
# out: assets/stu_second.glb
# walker_two      https://drive.google.com/file/d/XXXXXXXX/view
